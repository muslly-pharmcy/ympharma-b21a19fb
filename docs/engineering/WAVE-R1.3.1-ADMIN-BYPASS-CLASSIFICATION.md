# Wave R1.3.1 — Admin Client Elimination Audit

Generated: 2026-07-21T01:35:05.416Z

Auto-generated by `node scripts/audit-admin-bypass.mjs`. Extends
`WAVE-R1.3-AUTHZ-AUDIT.md` by classifying every `admin-bypass` handler
into one of four action buckets. **Read-only wave** — no code, RLS, or
migration changes are executed here.

## Executive summary

- Handlers scanned (admin-bypass verdict from R1.3): **91**

| Bucket | Meaning | Count |
|---|---|---:|
| **A** | Legitimate System Operations (keep) | 2 |
| **B** | Replace with RLS | 35 |
| **C** | Service Layer Required | 54 |
| **D** | Needs Redesign | 0 |

```text
Legitimate System Operations    2
Replace with RLS               35
Service Layer Required         54
Needs Redesign                  0
```

## Bucket definitions

- **A — Keep (legitimate bypass).** Must run above RLS by design:
  observability writes, AI runtime tables, cron/queue workers,
  audit-log writers, session bootstrap. Action: document, do not change.
- **B — Replace with RLS.** Ordinary tenant-scoped CRUD. The current
  `requirePermission` + `actor.organizationId` guard duplicates what a
  scoped RLS policy would enforce. Action (R1.3.3): write policy,
  migrate handler to `context.supabase`.
- **C — Service layer required.** Multi-table transactions, ledgers,
  state-machines, engine-backed workflows. RLS alone cannot express the
  invariant. Action (R1.3.4): extract to a `SECURITY DEFINER` RPC or
  an explicit `src/lib/services/` layer with tests around the invariant.
- **D — Redesign.** Auth/tenant check missing or executed *after* the
  mutation. Action (R1.3.5): fix order-of-operations or add guards
  before shipping any further work in that handler.

## Classification method

Applied to each admin-bypass handler in this priority order:

1. File is a pure system/observability surface (`ai.functions.ts`,
   `analytics.functions.ts`, `me.functions.ts`, `modules.functions.ts`) → **A**.
2. Handler writes to a system/observability table (`ai_events`, `agent_runs`,
   `audit_events`, `error_logs`, `air_*`, `inventory_idempotency`, etc.) → **A**.
3. Handler mutates but the first `requirePermission`/`requireOrg` appears
   *after* the first `.insert`/`.update`/`.delete`/`.upsert` → **D**.
4. Handler mutates but no `requirePermission` and no org check appear → **D**.
5. File is a domain-service surface (dispenses/loyalty/insurance/
   prescriptions/purchasing/campaigns/promotions mutations, sbdma-import),
   OR handler delegates to `engine.server`/`rule-engine`/`runtime/kernel`/
   `adapters.server`, OR uses `withIdempotency`, OR writes to ≥2 tables,
   OR performs a status transition → **C**.
6. Everything else = tenant-scoped CRUD that RLS could enforce → **B**.

## CRM cluster — 39

| File | Function | Bucket | Write tables | Read tables | Perm | Org | Rationale | Follow-up |
|---|---|:---:|---|---|:---:|:---:|---|---|
| `src/lib/campaigns.functions.ts` | `listCampaigns` | **B** | — | crm_campaigns | ✅ | ✅ | Tenant-scoped read on `crm_campaigns`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_campaigns` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/campaigns.functions.ts` | `getCampaign` | **B** | — | crm_campaigns, crm_campaign_events | ✅ | ✅ | Tenant-scoped read on `crm_campaigns`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_campaigns` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/campaigns.functions.ts` | `listSegments` | **B** | — | crm_segments | ✅ | ✅ | Tenant-scoped read on `crm_segments`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_segments` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/campaigns.functions.ts` | `getSegment` | **B** | — | crm_segments, crm_customers | ✅ | ✅ | Tenant-scoped read on `crm_segments`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_segments` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/campaigns.functions.ts` | `previewSegment` | **C** | — | crm_customers | ✅ | ✅ | Requires transactional / stateful invariant — delegates to engine.server. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/campaigns.mutations.functions.ts` | `upsertSegment` | **C** | crm_segments | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: campaigns.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/campaigns.mutations.functions.ts` | `recalcSegment` | **C** | — | crm_segments | ✅ | ✅ | Requires transactional / stateful invariant — domain: campaigns.mutations; delegates to engine.server. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/campaigns.mutations.functions.ts` | `createCampaign` | **C** | crm_campaigns | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: campaigns.mutations; idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/campaigns.mutations.functions.ts` | `updateCampaign` | **C** | crm_campaigns | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: campaigns.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/campaigns.mutations.functions.ts` | `scheduleCampaign` | **C** | crm_campaigns | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: campaigns.mutations; status transition. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/campaigns.mutations.functions.ts` | `transitionCampaign` | **C** | — | crm_campaigns | ✅ | ✅ | Requires transactional / stateful invariant — domain: campaigns.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/campaigns.mutations.functions.ts` | `startCampaign` | **C** | crm_campaign_runs, crm_campaign_recipients | crm_campaigns, crm_segments, crm_customers, crm_unsubscribes | ✅ | ✅ | Requires transactional / stateful invariant — domain: campaigns.mutations; delegates to engine.server; idempotent command; multi-table write (crm_campaign_runs, crm_campaign_recipients); status transition. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/customers.functions.ts` | `listCustomers` | **B** | — | crm_customers | ✅ | ✅ | Tenant-scoped read on `crm_customers`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_customers` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/customers.functions.ts` | `getCustomer` | **B** | — | crm_customers, crm_customer_contacts | ✅ | ✅ | Tenant-scoped read on `crm_customers`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_customers` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/customers.mutations.functions.ts` | `createCustomer` | **C** | crm_customers | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/customers.mutations.functions.ts` | `updateCustomer` | **B** | crm_customers | — | ✅ | ✅ | Tenant-scoped CRUD on `crm_customers`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_customers` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/customers.mutations.functions.ts` | `archiveCustomer` | **C** | crm_customers | — | ✅ | ✅ | Requires transactional / stateful invariant — status transition. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/customers.mutations.functions.ts` | `mergeCustomers` | **C** | crm_customer_addresses, crm_customer_tags | — | ✅ | ✅ | Requires transactional / stateful invariant — multi-table write (crm_customer_addresses, crm_customer_tags); status transition. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/customers.mutations.functions.ts` | `addCustomerAddress` | **B** | crm_customer_addresses | — | ✅ | ✅ | Tenant-scoped CRUD on `crm_customer_addresses`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_customer_addresses` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/customers.mutations.functions.ts` | `addCustomerContact` | **B** | crm_customer_contacts | — | ✅ | ✅ | Tenant-scoped CRUD on `crm_customer_contacts`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_customer_contacts` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/customers.mutations.functions.ts` | `addCustomerTag` | **B** | crm_customer_tags | — | ✅ | ✅ | Tenant-scoped CRUD on `crm_customer_tags`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_customer_tags` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/customers.mutations.functions.ts` | `removeCustomerTag` | **B** | crm_customer_tags | — | ✅ | ✅ | Tenant-scoped CRUD on `crm_customer_tags`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_customer_tags` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/loyalty.functions.ts` | `listLoyaltyAccounts` | **B** | — | crm_loyalty_accounts | ✅ | ✅ | Tenant-scoped read on `crm_loyalty_accounts`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_loyalty_accounts` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/loyalty.functions.ts` | `getLoyaltyAccount` | **B** | — | crm_loyalty_accounts, crm_reward_redemptions | ✅ | ✅ | Tenant-scoped read on `crm_loyalty_accounts`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_loyalty_accounts` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/loyalty.functions.ts` | `listLoyaltyTransactions` | **B** | — | crm_loyalty_transactions | ✅ | ✅ | Tenant-scoped read on `crm_loyalty_transactions`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_loyalty_transactions` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/loyalty.functions.ts` | `listRewards` | **B** | — | crm_reward_catalog | ✅ | ✅ | Tenant-scoped read on `crm_reward_catalog`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_reward_catalog` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/loyalty.functions.ts` | `listLoyaltyRules` | **B** | — | crm_loyalty_rules | ✅ | ✅ | Tenant-scoped read on `crm_loyalty_rules`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_loyalty_rules` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/loyalty.functions.ts` | `listLoyaltyTiers` | **B** | — | crm_loyalty_tiers | ✅ | ✅ | Tenant-scoped read on `crm_loyalty_tiers`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `crm_loyalty_tiers` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/loyalty.mutations.functions.ts` | `issuePoints` | **C** | — | crm_customers, crm_loyalty_rules | ✅ | ✅ | Requires transactional / stateful invariant — domain: loyalty.mutations; delegates to engine.server; idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/loyalty.mutations.functions.ts` | `reversePoints` | **C** | — | crm_loyalty_transactions | ✅ | ✅ | Requires transactional / stateful invariant — domain: loyalty.mutations; idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/loyalty.mutations.functions.ts` | `createReward` | **C** | crm_reward_catalog | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: loyalty.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/loyalty.mutations.functions.ts` | `redeemReward` | **C** | crm_reward_redemptions, crm_reward_catalog | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: loyalty.mutations; idempotent command; multi-table write (crm_reward_redemptions, crm_reward_catalog); status transition. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/loyalty.mutations.functions.ts` | `upsertLoyaltyRule` | **C** | crm_loyalty_rules | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: loyalty.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/promotions.mutations.functions.ts` | `createPromotion` | **C** | crm_promotions, crm_promotion_targets | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: promotions.mutations; idempotent command; multi-table write (crm_promotions, crm_promotion_targets). | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/promotions.mutations.functions.ts` | `updatePromotion` | **C** | crm_promotions, crm_promotion_eligibility | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: promotions.mutations; multi-table write (crm_promotions, crm_promotion_eligibility). | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/promotions.mutations.functions.ts` | `transitionPromotion` | **C** | crm_promotions | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: promotions.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/promotions.mutations.functions.ts` | `createCoupon` | **C** | crm_coupons, crm_coupon_codes | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: promotions.mutations; idempotent command; multi-table write (crm_coupons, crm_coupon_codes). | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/promotions.mutations.functions.ts` | `archiveCoupon` | **C** | crm_coupons | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: promotions.mutations; status transition. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/promotions.mutations.functions.ts` | `redeemCoupon` | **C** | — | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: promotions.mutations; idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |

## Medical cluster — 33

| File | Function | Bucket | Write tables | Read tables | Perm | Org | Rationale | Follow-up |
|---|---|:---:|---|---|:---:|:---:|---|---|
| `src/lib/clinical.functions.ts` | `runClinicalCheckForPrescription` | **C** | — | hc_prescriptions, hc_prescription_items, patient_conditions | ✅ | ✅ | Requires transactional / stateful invariant — delegates to engine.server. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/dispenses.functions.ts` | `listDispenses` | **B** | — | hc_dispenses | ✅ | ✅ | Tenant-scoped read on `hc_dispenses`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_dispenses` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/dispenses.functions.ts` | `listPendingDispenses` | **B** | — | hc_dispenses | ✅ | ✅ | Tenant-scoped read on `hc_dispenses`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_dispenses` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/dispenses.functions.ts` | `getDispense` | **B** | — | hc_dispenses, hc_dispense_items | ✅ | ✅ | Tenant-scoped read on `hc_dispenses`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_dispenses` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/dispenses.mutations.functions.ts` | `createDispense` | **C** | hc_prescription_items, hc_dispense_items | hc_prescriptions | ✅ | ✅ | Requires transactional / stateful invariant — domain: dispenses.mutations; idempotent command; multi-table write (hc_prescription_items, hc_dispense_items). | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/dispenses.mutations.functions.ts` | `prepareDispense` | **C** | inv_reservations | hc_dispense_items | ✅ | ✅ | Requires transactional / stateful invariant — domain: dispenses.mutations; idempotent command; status transition. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/dispenses.mutations.functions.ts` | `verifyDispenseItemBarcode` | **C** | hc_dispense_items | catalog_barcodes, inv_stock_batches | ✅ | — | Requires transactional / stateful invariant — domain: dispenses.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/dispenses.mutations.functions.ts` | `verifyDispense` | **C** | — | hc_dispense_items | ✅ | ✅ | Requires transactional / stateful invariant — domain: dispenses.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/dispenses.mutations.functions.ts` | `dispensePrescription` | **C** | hc_dispense_items | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: dispenses.mutations; idempotent command; status transition. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/dispenses.mutations.functions.ts` | `cancelDispense` | **C** | hc_dispense_items | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: dispenses.mutations; status transition. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/dispenses.mutations.functions.ts` | `returnDispense` | **C** | hc_dispense_returns | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: dispenses.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/doctors.functions.ts` | `listDoctors` | **B** | — | hc_doctors | ✅ | ✅ | Tenant-scoped read on `hc_doctors`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_doctors` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/doctors.functions.ts` | `getDoctor` | **B** | — | hc_doctors | ✅ | ✅ | Tenant-scoped read on `hc_doctors`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_doctors` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/doctors.mutations.functions.ts` | `createDoctor` | **C** | hc_doctors | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/doctors.mutations.functions.ts` | `updateDoctor` | **B** | hc_doctors | — | ✅ | ✅ | Tenant-scoped CRUD on `hc_doctors`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_doctors` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/doctors.mutations.functions.ts` | `addLicense` | **B** | hc_doctors | — | ✅ | ✅ | Tenant-scoped CRUD on `hc_doctors`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_doctors` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/insurance.mutations.functions.ts` | `decideAuthorization` | **C** | insv2_authorizations, insv2_claims | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: insurance.mutations; multi-table write (insv2_authorizations, insv2_claims). | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/patients.functions.ts` | `listPatients` | **B** | — | hc_patients | ✅ | ✅ | Tenant-scoped read on `hc_patients`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_patients` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/patients.functions.ts` | `getPatient` | **B** | — | hc_patients, patient_conditions | ✅ | ✅ | Tenant-scoped read on `hc_patients`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_patients` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/patients.mutations.functions.ts` | `createPatient` | **C** | hc_patients | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/patients.mutations.functions.ts` | `updatePatient` | **B** | hc_patients | — | ✅ | ✅ | Tenant-scoped CRUD on `hc_patients`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_patients` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/patients.mutations.functions.ts` | `addAllergy` | **B** | patient_allergies | — | ✅ | ✅ | Tenant-scoped CRUD on `patient_allergies`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `patient_allergies` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/patients.mutations.functions.ts` | `addCondition` | **B** | patient_conditions | — | ✅ | ✅ | Tenant-scoped CRUD on `patient_conditions`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `patient_conditions` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/patients.mutations.functions.ts` | `addEmergencyContact` | **B** | patient_emergency_contacts | — | ✅ | ✅ | Tenant-scoped CRUD on `patient_emergency_contacts`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `patient_emergency_contacts` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/patients.mutations.functions.ts` | `mergePatients` | **B** | hc_patients | — | ✅ | ✅ | Tenant-scoped CRUD on `hc_patients`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_patients` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/prescriptions.functions.ts` | `listPrescriptions` | **B** | — | hc_prescriptions | ✅ | ✅ | Tenant-scoped read on `hc_prescriptions`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_prescriptions` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/prescriptions.functions.ts` | `getPrescription` | **B** | — | hc_prescriptions, hc_prescription_items | ✅ | ✅ | Tenant-scoped read on `hc_prescriptions`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `hc_prescriptions` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/prescriptions.mutations.functions.ts` | `createPrescription` | **C** | hc_prescriptions, hc_prescription_items, hc_prescription_status_history | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: prescriptions.mutations; idempotent command; multi-table write (hc_prescriptions, hc_prescription_items, hc_prescription_status_history). | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/prescriptions.mutations.functions.ts` | `updatePrescription` | **C** | hc_prescriptions | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: prescriptions.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/prescriptions.mutations.functions.ts` | `addPrescriptionItem` | **C** | hc_prescription_items | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: prescriptions.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/prescriptions.mutations.functions.ts` | `removePrescriptionItem` | **C** | hc_prescription_items | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: prescriptions.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/prescriptions.mutations.functions.ts` | `transitionPrescription` | **C** | hc_prescription_items, hc_prescription_status_history | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: prescriptions.mutations; multi-table write (hc_prescription_items, hc_prescription_status_history). | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/prescriptions.mutations.functions.ts` | `addPrescriptionNote` | **C** | hc_prescription_notes | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: prescriptions.mutations. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |

## Supply cluster — 17

| File | Function | Bucket | Write tables | Read tables | Perm | Org | Rationale | Follow-up |
|---|---|:---:|---|---|:---:|:---:|---|---|
| `src/lib/catalog.mutations.functions.ts` | `createProduct` | **C** | catalog_products | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/catalog.mutations.functions.ts` | `updateProduct` | **C** | catalog_products | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/catalog.mutations.functions.ts` | `archiveProduct` | **C** | — | catalog_products | ✅ | ✅ | Requires transactional / stateful invariant — status transition. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/inventory.mutations.functions.ts` | `createWarehouse` | **C** | wh_warehouses | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/inventory.mutations.functions.ts` | `updateWarehouse` | **B** | wh_warehouses | — | ✅ | ✅ | Tenant-scoped CRUD on `wh_warehouses`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `wh_warehouses` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |
| `src/lib/inventory.mutations.functions.ts` | `receiveStock` | **C** | — | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/inventory.mutations.functions.ts` | `adjustStock` | **C** | — | — | ✅ | — | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/inventory.mutations.functions.ts` | `transferStock` | **C** | — | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/inventory.mutations.functions.ts` | `reserveStock` | **C** | — | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/inventory.mutations.functions.ts` | `releaseReservation` | **C** | — | — | ✅ | — | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/inventory.mutations.functions.ts` | `consumeReservation` | **C** | — | — | ✅ | — | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/inventory.mutations.functions.ts` | `returnStock` | **C** | — | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/purchasing.functions.ts` | `createPurchaseOrder` | **C** | purchase_orders, purchase_order_lines | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: purchasing; idempotent command; multi-table write (purchase_orders, purchase_order_lines). | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/purchasing.functions.ts` | `updatePurchaseOrder` | **C** | purchase_orders | — | ✅ | ✅ | Requires transactional / stateful invariant — domain: purchasing. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/purchasing.functions.ts` | `receivePurchaseOrder` | **C** | — | — | ✅ | — | Requires transactional / stateful invariant — domain: purchasing. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/suppliers.mutations.functions.ts` | `createSupplier` | **C** | sup_suppliers | — | ✅ | ✅ | Requires transactional / stateful invariant — idempotent command. | Extract to SECURITY DEFINER RPC or src/lib/services/ layer with explicit invariants + tests. |
| `src/lib/suppliers.mutations.functions.ts` | `updateSupplier` | **B** | sup_suppliers | — | ✅ | ✅ | Tenant-scoped CRUD on `sup_suppliers`; org filter + role check duplicate what an RLS policy would enforce. | Add RLS policy on `sup_suppliers` scoped to `organization_id = current_org()`, then migrate handler to context.supabase. |

## Platform cluster — 2

| File | Function | Bucket | Write tables | Read tables | Perm | Org | Rationale | Follow-up |
|---|---|:---:|---|---|:---:|:---:|---|---|
| `src/lib/ai.functions.ts` | `listAgents` | **A** | — | air_agents | — | — | System/observability surface (ai.functions.ts); runs above RLS by design. | Document the bypass in a header comment; no code change. |
| `src/lib/ai.functions.ts` | `listRuns` | **A** | — | air_runs | — | ✅ | System/observability surface (ai.functions.ts); runs above RLS by design. | Document the bypass in a header comment; no code change. |

## Follow-up backlog (sequenced, not executed here)

- **R1.3.2 — RLS policy migration.** For every Bucket B row, write the
  missing tenant-scoped SELECT/INSERT/UPDATE/DELETE policies on the
  target table. Ship as one migration per domain cluster.
- **R1.3.3 — Bucket B handler migration.** After R1.3.2 lands, migrate
  each Bucket B handler from `supabaseAdmin` to `context.supabase` +
  `requireSupabaseAuth`. Keep the existing `requirePermission` as a
  defence-in-depth layer; RLS becomes the enforcement plane.
- **R1.3.4 — Bucket C service extraction.** For every Bucket C row,
  choose either a `SECURITY DEFINER` RPC (when the invariant fits SQL)
  or an explicit `src/lib/services/<domain>-service.server.ts` module
  with unit tests around the invariant.
- **R1.3.5 — Bucket D repair.** Fix the order-of-operations / missing-
  guard issue in the flagged handlers before any other work on that
  domain.
- **R1.4 — Server function contract audit.** Zod coverage, error taxonomy,
  correlation IDs, pagination, idempotency, timeouts. Queued.
- **R1.5 — Domain boundary audit.** Import graphs, circular deps,
  domain leakage. Queued.
- **R1.6 — Secrets & configuration audit.** `VITE_*` leakage check,
  boot-time secret presence assertions. Queued.

## Non-goals (explicit)

- No migrations run in this wave.
- No handler rewrites.
- No changes to `session.server.ts`, `idempotency.server.ts`, or the
  `_authenticated/` route gate.
- Tracks B/C (WhatsApp/Meta) remain gated on Lovable Secrets.